---
# Test for custom ovh modules

- hosts: localhost
  tasks:
    - name: Test with a message
      ovh_server:
        service: name
        template: foo
        hostname: bar
        ssh_key: pub
        distrib_kernel: true
